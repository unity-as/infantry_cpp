# Core/ —— CubeMX 生成区

此目录由 CubeMX 生成（`Inc / Src / Startup`），不要手动修改。

生成方式：CubeMX 打开 `.ioc` → Project Manager → Code Generator 勾选
“Generate peripheral initialization as a pair of '.c/.h' files per peripheral”。
