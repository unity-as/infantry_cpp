# AI_GUARDRAILS.md — AI 护栏（最高优先级）

> 本文件约束 AI 在本项目内的一切代码/配置改动，优先级高于任何功能需求。
> 与 PROJECT_MAP.md 同级，AI 开局先读本文件再读地图。

## A. 硬件安全原则（禁止破坏）
本项目的硬件保护设计（温控/超温/过流/看门狗/电源等）以代码与原理图为准，AI 改动时：
1. **危险外设默认不启动**。加热/电机/PWM 功率输出等，改动不得让其在未确认安全的情况下意外开启。
2. **保护阈值与检查不得削弱**：超温复位、过流断电、锁定停机、安全上限等，不得调高阈值、移除检查、或让主循环跳过。
3. **看门狗保持兜底**：由主循环喂狗；不得擅自关闭/延长到失去兜底意义。
4. **断点冻结风险**：调试器 halt 可能冻结外设时钟（如 DBGMCU 冻结 TIM），PWM 可能锁在当前电平，功率输出期间打点有烧毁风险。优先用 ai-debug 的**不停机采样**（sample 命令）观察变量；必须 halt 时先关闭功率输出或人工确认断电。
5. **先确认供电前提**：主供电 vs 调试供电要分清。功率/温度上不去时先查供电，不先怀疑/改代码。

## B. AI 变更纪律（违反即事故）
1. **所有改动必须显式报告**。每次改动后必须列出：改了哪个文件、哪几行、改前→改后、为什么。禁止"顺手"改与任务无关的东西。用户有权要求任何一次改动的完整 diff。
2. **禁止私自改硬件/通信配置**。波特率、时钟、引脚、外设参数、.ioc 配置等，一律先说明原因和方案，经确认后才动。发现"通信不上/外设异常"时先诊断并报告，**严禁直接改配置试错**——默认假设是配置没错，问题在别处。
3. **改 A 之前先留基线，实现 B 之后必须验证 A 没坏**。基线可以是：RTT 输出快照、ai-debug 读取的关键变量值、或一组可复现的行为。B 完成后逐项对比 A 的基线。A 被破坏且无人发现 = 最严重事故。
4. **一次只做一个任务**。不混改；每个改动可回滚（git/备份）。
5. **不隐藏失败**。改完发现不工作，如实报告现象和证据，不掩盖、不绕道、不删日志。


## B2. 删除纪律（最高优先级，防止误删）
1. **AI 默认无权递归删除**。任何删除文件夹/多文件的操作，必须先向用户列出：目标路径、将删的内容清单、影响范围，经确认后才可执行。
2. **优先可逆操作**。能移动（到回收站/临时目录）就不删除；能改名就不覆盖。
3. **运行可能产生副作用的脚本前，先说明**：该脚本会写/删哪些路径。若脚本含递归删除或覆盖逻辑，必须先检查目标目录内容，确认不是已有数据。
4. **禁止"顺手清理"**。未在任务范围内、未经用户要求的删除一律不做，即使看起来"没用"或"临时"。
5. **删除后报告**。执行任何删除后，简述删了什么、是否可恢复。

## C. 护栏的强制执行
- 写操作默认先给用户看 diff 摘要，确认后再落地；配置类文件（.ioc / *.cfg / 波特率宏）AI 默认无权改，只能提建议。
- 任何"先改了试试"的冲动，都先转为"先诊断、给证据、提方案"。
- 本文件本身如需修改，须用户明确同意。