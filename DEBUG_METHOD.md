# DEBUG_METHOD.md — 灵异问题处理手册

> 适用：现象违背直觉、常规排查全部失效、所有"应该正确"的东西看起来都正确，但结果就是不对。
> 不限于硬件：软件、数据、流程、环境都适用。
> 核心信念：灵异问题不是没有原因，而是原因藏在"我们认为理所当然不会错"的假设里。

## 0. 先立心态
- 问题神秘 = 我的模型有盲区，不是世界坏了。
- 最危险的不是查不出，而是被焦虑推着"换一个试试"。
- 你才是负责人：损坏的硬件、浪费的时间、错过的期限，别人都不用承担。

## 1. 取证优先，猜测靠后
- 任何"换一个 / 改一下 / 重来一遍"之前，先回答：
  "我要验证什么假设？这个操作能区分哪两种可能？"
- 答不上来 = 不许动手。把"试试"改成"实验"。

## 2. 把系统拆成因果链，切半定位
- 现象在链上任何位置都成立，所以"就是不对"不能定位。
- 排查 = 沿链给每个环节做体检，用"这环节正常/异常"把链切两半。
- 链可以是硬件链路、代码调用链、数据流、业务流程，任何东西。
- 每轮只做一个实验，实验必须给出"是/否"的明确结论。

## 3. 利用变化信息
- "加了 X 之后 Y 坏了" → 第一嫌疑永远是 X，哪怕看起来无关。
- 回退对比（git/备份）是最快的定位工具：回到能用的版本，二分找破坏点。
- 没有变化就出问题 → 嫌疑转向环境：供电、温度、接触、时序、外部依赖。

## 4. 断言必须验证
- "好了"和"没问题"都是断言，和"有问题"一样需要证据。
- 回退/修改后，必须用实测确认恢复，而不是"应该没事"。
- 别人（包括 AI）说"没问题"时，先问：它看了什么？证据是什么？
  没看代码就说没问题 = 无效断言。

## 5. 保持判断权
- 外部建议（AI/同事/论坛）带着自信的外表，但验证只能自己做。
- 任何结论，能一句话解释给队友听才算理解；解释不清 = 先别动。
- 不可逆操作（换硬件、擦除、覆盖、删数据）只能由你决策。

## 6. 反模式清单（禁止）
- 把"换一个试试"当排查手段
- 不做实验就下结论，或未经验证就断言"没问题"
- 加功能破坏系统后不从改动里找原因
- 回退后不验证就宣称修好
- 重复同类失败操作（上次没用，换汤不换药再来一次）
- 让外部工具/人决定不可逆操作

## 7. 工具箱（按场景选）
- 最小复现：删到只剩问题本身
- 回退对比：git/备份，二分定位
- 切半实验：回环、桩、隔离依赖、A/B 对比
- 观测手段：日志、波形、读数、状态快照、断点
- 权威参照：规格书、文档、已知良好样本